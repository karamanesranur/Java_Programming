package Java_Programming_Muhtar.day006_IfStatements;

public class MedNumber {
    public static void main(String[] args) {

      /*  int a = 5;
        int b = 30;
        int c = 20;

        boolean aIsMedian = (b > a && a > c) || (c > a && a > b);
        // || : OR operators only One condition need to be true in order to
        // in order for a to be median number between three DIFFERENT number
        // a need to be less than b and greater than c . Or a need to be less than c and greater than b
        boolean bIsMedian = !aIsMedian && (c > b || c > a);
        // boolean aIsMedian = (b< a && b > c) || (c >b && b>a) ;(opisyonel)
        boolean cIsMedian = !aIsMedian && !bIsMedian
        // if both a and b are not median number between three different numbers. then c must be the median number



        if (aIsMedian) {
            System.out.println(a + " is the median number");
        } else {
            System.out.println("not median number");
        } */

    }
}
